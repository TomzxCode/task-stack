[project]
name = "task-stack"
version = "0.1.0"
description = "A persistent task-stack tray app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mkdocs>=1.5.0",
    "mkdocs-material[imaging]>=9.4.0",
]
